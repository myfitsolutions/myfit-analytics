(() => {"use strict";document.getElementById("onboarding-form").addEventListener("submit",async event=>{event.preventDefault();const status=document.getElementById("onboarding-status");const value=id=>document.getElementById(id).value;const payload={name:value("ob-name"),timezone:value("ob-timezone"),currency:value("ob-currency").trim().toUpperCase(),retention_healthy_days:Number(value("ob-healthy")),retention_watch_days:Number(value("ob-watch")),retention_at_risk_days:Number(value("ob-risk")),default_follow_up_days:Number(value("ob-followup")),sender_name:null,platform:document.querySelector("input[name='platform']:checked").value,destination:document.querySelector("input[name='destination']:checked").value};try{status.textContent="Saving studio setup...";const response=await fetch("/onboarding/complete",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(payload)});const data=await response.json();if(!response.ok)throw new Error(typeof data.detail==="string"?data.detail:"Unable to complete setup");window.location.assign(data.redirect_to);}catch(error){status.textContent=error.message;status.className="settings-status settings-status-error";}});})();
